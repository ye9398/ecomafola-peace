# Claude Code Step 1: TDD 开发

You are working on the ecomafola-peace project (Vite + React + Shopify e-commerce).

## Your Task (3 fixes via TDD pattern)

### Fix 1: AVIF Detection
File: src/lib/imageOptimizer.ts
Change shouldUseAVIF() from UA sniffing to CSS.supports('image-type', 'image/avif')
The current function uses navigator.userAgent regex patterns. Replace with proper feature detection.

### Fix 2: Data Deduplication  
File: src/lib/dataFetcher.ts
In fetchHomePageData(), newProducts calls getProducts(featuredCount) which returns the SAME products as featuredProducts.
Fix: newProducts should skip the first featuredCount products using slice(featuredCount).

### Fix 3: Lazy Loading Routes
File: src/App.tsx
Integrate lazy loading from src/components/LazyLoading.tsx:
- ProductDetailPage -> LazyProductDetailPage + ProductDetailSuspense
- CheckoutPage -> LazyCheckoutPage + CheckoutSuspense
- AccountOrdersPage -> LazyAccountOrdersPage + SuspenseBoundary

## Requirements
1. Write tests FIRST, then implement (TDD pattern)
2. Run: npx vitest run  (verify tests pass)
3. Run: npm run build    (verify build succeeds)
4. After all 3 fixes, write a summary to:
   .ai-memory/step1-tdd.md
   Include: what files changed, test results, build status, issues encountered
